const { Contact, validationContact, validationFavorite } = require("./contact");
const {
  User,
  validationRegistrationUser,
  validationLoginUser,
} = require("./user");

module.exports = {
  Contact,
  validationContact,
  validationFavorite,
  User,
  validationRegistrationUser,
  validationLoginUser,
};
